# Initial Drop Animation

- [x] Modify `HolographicID.astro` Physics Initialization
    - [x] Set initial `pos.y` to a negative value (above screen).
    - [x] Add slight random `pos.x` or `vel.x` to induce initial sway (swinging).
    - [x] Ensure physics loop starts immediately.
